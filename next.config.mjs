/** @type {import('next').NextConfig} */
const nextConfig = {
  // Preparado para desplegar directamente en Vercel y asociar laludotecadetauste.com cuando esté comprado.
};

export default nextConfig;
